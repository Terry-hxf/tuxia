<?xml version="1.0" encoding="utf-8" standalone="yes"?><rss version="2.0" xmlns:atom="http://www.w3.org/2005/Atom"><channel><title>Tools on TUXIA - Free Online Image Tools for Compression, Conversion, Crop and Watermark</title><link>https://tuxiatools.com/tools/index.html</link><description>Recent content in Tools on TUXIA - Free Online Image Tools for Compression, Conversion, Crop and Watermark</description><generator>Hugo</generator><language>en-US</language><atom:link href="https://tuxiatools.com/tools/index.xml" rel="self" type="application/rss+xml"/><item><title>EXIF Viewer | View Photo Metadata and Camera Info Online - TUXIA</title><link>https://tuxiatools.com/tools/exif.html</link><pubDate>Mon, 01 Jan 0001 00:00:00 +0000</pubDate><guid>https://tuxiatools.com/tools/exif.html</guid><description>&lt;style&gt;
.tool-container { max-width: 700px; margin: 0 auto; padding: 32px 24px; }
 .exif-grid { display: none; grid-template-columns: 1fr 1fr; gap: 14px; margin-top: 20px; }
 @media (max-width: 500px) { .exif-grid { grid-template-columns: 1fr; } }
 .exif-grid.visible { display: grid; }
 .exif-item { padding: 12px 14px; background: var(--surface); border: 1px solid var(--border); border-radius: var(--radius-sm); display: flex; justify-content: space-between; align-items: center; }
 .exif-label { font-size: 0.78rem; color: var(--text-muted); }
 .exif-value { font-size: 0.85rem; font-weight: 500; text-align: right; max-width: 60%; word-break: break-all; }
 .exif-none { text-align: center; color: var(--text-muted); font-size: 0.9rem; margin-top: 16px; display: none; }
 .exif-none.visible { display: block; }
 .preview-area { text-align: center; margin-top: 18px; display: none; }
 .preview-area img { max-width: 260px; max-height: 200px; border-radius: var(--radius); border: 1px solid var(--border); }
&lt;/style&gt;

&lt;div class="tool-container"&gt;
 &lt;div class="section-header"&gt;
 &lt;p class="section-label"&gt;Metadata&lt;/p&gt;</description></item><item><title>Free Image Compressor | Compress JPG PNG WebP Online - TUXIA</title><link>https://tuxiatools.com/tools/compress.html</link><pubDate>Mon, 01 Jan 0001 00:00:00 +0000</pubDate><guid>https://tuxiatools.com/tools/compress.html</guid><description>&lt;style&gt;
.tool-container { max-width: 900px; margin: 0 auto; padding: 32px 24px; }
 .compare-layout { display: grid; grid-template-columns: 1fr 1fr; gap: 24px; }
 @media (max-width: 700px) { .compare-layout { grid-template-columns: 1fr; } }
 .compare-panel { text-align: center; padding: 18px; border: 1px solid var(--border); border-radius: var(--radius); background: var(--surface); }
 .compare-panel h3 { font-size: 0.9rem; margin-bottom: 12px; color: var(--text-secondary); }
 .compare-panel img { max-width: 100%; max-height: 260px; border-radius: var(--radius-sm); border: 1px solid var(--border); }
 .compare-panel .file-info { font-size: 0.78rem; color: var(--text-muted); margin-top: 8px; }
 .compare-panel .file-info .saved { color: var(--success); font-weight: 600; }
 .compare-panel .file-info .neutral { color: var(--text-secondary); font-weight: 600; }
 .quality-control { margin: 20px 0; padding: 16px; background: var(--surface); border: 1px solid var(--border); border-radius: var(--radius); }
 .quality-control label { font-size: 0.85rem; font-weight: 500; display: flex; justify-content: space-between; align-items: center; }
 .quality-control input[type="range"] { width: 100%; margin-top: 8px; }
&lt;/style&gt;

&lt;div class="tool-container"&gt;
 &lt;div class="section-header"&gt;
 &lt;p class="section-label"&gt;Compression&lt;/p&gt;</description></item><item><title>Image Color Picker | Extract HEX RGB HSL Palette Online - TUXIA</title><link>https://tuxiatools.com/tools/palette.html</link><pubDate>Mon, 01 Jan 0001 00:00:00 +0000</pubDate><guid>https://tuxiatools.com/tools/palette.html</guid><description>&lt;style&gt;
.tool-container { max-width: 980px; margin: 0 auto; padding: 32px 24px; }
 #palette-tool.has-result {
 flex: 1 0 auto;
 min-height: clamp(580px, calc(100vh - 396px), 720px);
 padding-bottom: 16px;
 }
 #palette-tool.has-result .upload-zone { padding: 16px; }
 .palette-result { display: none; margin-top: 12px; }
 .palette-result.visible { display: flex; flex: 1 1 auto; min-height: 0; }
 #palette-tool.has-result .palette-workspace { display: grid; grid-template-columns: minmax(280px, 340px) 1fr; gap: 18px; align-items: stretch; width: 100%; min-height: 0; flex: 1 1 auto; }
 .palette-board { border: 1px solid var(--border); border-radius: var(--radius); background: var(--surface); padding: 16px; box-shadow: var(--shadow-sm); display: flex; flex-direction: column; min-height: 0; }
 .palette-board-head { display: flex; justify-content: space-between; align-items: center; gap: 12px; margin-bottom: 12px; }
 .palette-board-copy { min-width: 0; }
 .palette-board-title { font-size: 0.92rem; font-weight: 750; color: var(--text); }
 .palette-board-subtitle { margin-top: 2px; color: var(--text-muted); font-size: 0.72rem; }
 .palette-strip { display: grid; grid-template-columns: repeat(6, minmax(0, 1fr)); gap: 7px; margin-bottom: 12px; }
 .palette-strip button { height: 34px; border: 1px solid var(--border); border-radius: var(--radius-sm); cursor: pointer; box-shadow: inset 0 1px 0 rgba(255,255,255,0.2); }
 #palette-tool.has-result .palette-preview-card { border: 1px solid var(--border); border-radius: var(--radius); background: var(--surface); padding: 14px; box-shadow: var(--shadow-sm); height: 100%; display: flex; flex-direction: column; min-height: 0; }
 .palette-picker-stage { position: relative; flex: 1 1 auto; min-height: 260px; border-radius: var(--radius-sm); overflow: hidden; background: linear-gradient(180deg, #fbfdff 0%, #f3f8ff 100%); cursor: crosshair; display: flex; align-items: center; justify-content: center; }
 #palette-tool.has-result .palette-preview-card img { width: 100%; height: 100%; max-height: none; object-fit: contain; display: block; }
 .picker-loupe { position: absolute; z-index: 3; display: none; align-items: center; gap: 8px; min-width: 112px; padding: 7px 9px; border: 1px solid rgba(15,23,42,0.12); border-radius: var(--radius-sm); background: rgba(255,255,255,0.94); box-shadow: 0 10px 26px rgba(15,23,42,0.14); pointer-events: none; transform: translate(12px, 12px); }
 .picker-loupe.visible { display: flex; }
 .picker-swatch { width: 22px; height: 22px; border-radius: 5px; border: 1px solid var(--border); flex: 0 0 auto; }
 .picker-code { font-family: Consolas, "Liberation Mono", monospace; font-size: 0.72rem; font-weight: 750; color: var(--text); white-space: nowrap; }
 .picker-current { display: flex; align-items: center; justify-content: space-between; gap: 10px; margin-top: 9px; padding: 8px 10px; border: 1px solid var(--border); border-radius: var(--radius-sm); background: #f8fbff; }
 .picker-current-main { display: flex; align-items: center; gap: 8px; min-width: 0; }
 .picker-current-label { color: var(--text-muted); font-size: 0.7rem; }
 .picker-current-code { font-family: Consolas, "Liberation Mono", monospace; font-size: 0.76rem; font-weight: 750; color: var(--text); }
 .palette-preview-meta { display: flex; justify-content: space-between; gap: 10px; margin-top: 10px; font-size: 0.76rem; color: var(--text-muted); }
 .palette-preview-card .palette-preview-meta { margin-top: auto; padding-top: 10px; }
 .palette-toolbar { display: flex; justify-content: space-between; align-items: center; gap: 14px; flex-wrap: wrap; margin-bottom: 14px; }
 .format-tabs { display: inline-flex; gap: 4px; padding: 4px; border: 1px solid var(--border); border-radius: var(--radius); background: #f8fafc; }
 .format-tab { border: 0; border-radius: var(--radius-sm); padding: 7px 11px; background: transparent; color: var(--text-secondary); font-size: 0.76rem; font-weight: 600; cursor: pointer; }
 .format-tab.active { background: var(--surface); color: var(--accent); box-shadow: var(--shadow-sm); }
 #palette-tool.has-result .palette-grid { display: grid; grid-template-columns: repeat(3, minmax(0, 1fr)); grid-template-rows: repeat(2, minmax(118px, 1fr)); gap: 10px; flex: 1 1 auto; min-height: 0; }
 .color-card { min-height: 0; border: 1px solid var(--border); border-radius: var(--radius); background: var(--surface); overflow: hidden; box-shadow: var(--shadow-sm); transition: transform 160ms ease, border-color 160ms ease, box-shadow 160ms ease; display: flex; flex-direction: column; }
 .color-card:hover { transform: translateY(-2px); border-color: #93c5fd; box-shadow: 0 12px 26px rgba(37,99,235,0.1); }
 .color-swatch { flex: 0 0 46%; min-height: 48px; cursor: pointer; position: relative; }
 .color-swatch::after { content: "Click to copy"; position: absolute; right: 10px; top: 10px; padding: 3px 8px; border-radius: var(--radius-full); background: rgba(0,0,0,0.46); color: #fff; font-size: 0.68rem; font-weight: 600; opacity: 0; transform: translateY(-2px); transition: opacity 160ms ease, transform 160ms ease; }
 .color-card:hover .color-swatch::after { opacity: 1; transform: translateY(0); }
 .color-card.light .color-swatch::after { background: rgba(255,255,255,0.76); color: var(--text); }
 .color-info { padding: 10px 12px 12px; flex: 1 1 auto; display: flex; flex-direction: column; justify-content: center; min-height: 0; }
 .color-main { display: flex; justify-content: space-between; align-items: center; gap: 8px; margin-bottom: 8px; }
 .color-code { min-width: 0; font-family: Consolas, "Liberation Mono", monospace; font-size: 0.78rem; font-weight: 700; color: var(--text); white-space: nowrap; overflow: hidden; text-overflow: ellipsis; }
 .copy-mini { width: 30px; height: 30px; border: 1px solid var(--border); border-radius: var(--radius-sm); background: var(--surface); color: var(--text-muted); display: inline-flex; align-items: center; justify-content: center; cursor: pointer; flex: 0 0 auto; }
 .copy-mini:hover { border-color: var(--accent); color: var(--accent); background: var(--accent-subtle); }
 .color-sub { display: grid; gap: 4px; font-family: Consolas, "Liberation Mono", monospace; font-size: 0.68rem; color: var(--text-muted); }
 .palette-empty-note { display: flex; align-items: center; gap: 8px; justify-content: center; margin-top: 10px; color: var(--text-muted); font-size: 0.76rem; }
 @media (max-width: 820px) {
 #palette-tool.has-result { min-height: 0; }
 #palette-tool.has-result .palette-workspace { grid-template-columns: 1fr; }
 #palette-tool.has-result .palette-preview-card { max-width: 420px; margin: 0 auto; width: 100%; min-height: 380px; }
 }
 @media (max-width: 560px) {
 .tool-container { padding-left: 18px; padding-right: 18px; }
 .palette-grid { grid-template-columns: 1fr; }
 .palette-board-head { align-items: stretch; flex-direction: column; }
 .palette-toolbar { align-items: stretch; }
 .format-tabs { width: 100%; }
 .format-tab { flex: 1; }
 }
&lt;/style&gt;

&lt;div class="tool-container" id="palette-tool"&gt;
 &lt;div class="section-header"&gt;
 &lt;p class="section-label"&gt;Color analysis&lt;/p&gt;</description></item><item><title>Image Format Converter | PNG JPG WebP BMP SVG Online - TUXIA</title><link>https://tuxiatools.com/tools/convert.html</link><pubDate>Mon, 01 Jan 0001 00:00:00 +0000</pubDate><guid>https://tuxiatools.com/tools/convert.html</guid><description>&lt;style&gt;
.tool-container { max-width: 680px; margin: 0 auto; padding: 32px 24px; }
 .format-options { display: flex; gap: 10px; flex-wrap: wrap; margin: 16px 0; }
 .format-opt { padding: 8px 18px; border: 1px solid var(--border); border-radius: var(--radius-sm); cursor: pointer; font-size: 0.85rem; font-weight: 500; background: var(--surface); transition: all var(--transition); }
 .format-opt:hover { border-color: var(--accent); }
 .format-opt.active { border-color: var(--accent); background: var(--accent-subtle); color: var(--accent); }
 .convert-result { text-align: center; padding: 18px; margin-top: 18px; display: none; }
 .convert-result img { max-width: 100%; max-height: 260px; border-radius: var(--radius-sm); border: 1px solid var(--border); }
&lt;/style&gt;

&lt;div class="tool-container"&gt;
 &lt;div class="section-header"&gt;
 &lt;p class="section-label"&gt;Format conversion&lt;/p&gt;</description></item><item><title>Image Resizer | Resize Photos by Width, Height or Scale - TUXIA</title><link>https://tuxiatools.com/tools/resize.html</link><pubDate>Mon, 01 Jan 0001 00:00:00 +0000</pubDate><guid>https://tuxiatools.com/tools/resize.html</guid><description>&lt;style&gt;
 .tool-container { max-width: 920px; margin: 0 auto; padding: 30px 24px; }
 .resize-workspace { display: none; grid-template-columns: minmax(360px, 1fr) 310px; gap: 16px; align-items: stretch; margin-top: 18px; }
 .resize-workspace.visible { display: grid; }
 .resize-preview-card,
 .resize-control-card { border: 1px solid var(--border); border-radius: var(--radius); background: var(--surface); box-shadow: var(--shadow-sm); }
 .resize-preview-card { padding: 14px; display: flex; flex-direction: column; gap: 10px; min-height: 410px; }
 .resize-canvas-wrap { flex: 1 1 auto; min-height: 310px; border: 1px solid var(--border); border-radius: var(--radius-sm); background: linear-gradient(180deg, #fbfdff 0%, #f4f8ff 100%); display: flex; align-items: center; justify-content: center; overflow: hidden; }
 #resize-canvas { max-width: 100%; max-height: 500px; display: block; }
 .resize-meta { display: flex; justify-content: space-between; gap: 10px; color: var(--text-muted); font-size: 0.76rem; }
 .resize-control-card { padding: 16px; display: grid; gap: 14px; align-content: start; }
 .resize-field { display: grid; gap: 7px; }
 .resize-field label,
 .resize-control-label { color: var(--text-secondary); font-size: 0.78rem; font-weight: 650; }
 .resize-field input,
 .resize-field select { width: 100%; height: 38px; border: 1px solid var(--border); border-radius: var(--radius-sm); background: var(--surface); color: var(--text); padding: 0 10px; outline: none; }
 .resize-field input:focus,
 .resize-field select:focus { border-color: var(--accent); box-shadow: 0 0 0 3px rgba(37,99,235,0.12); }
 .resize-size-grid { display: grid; grid-template-columns: 1fr 1fr; gap: 10px; }
 .resize-check { display: flex; align-items: center; gap: 9px; color: var(--text-secondary); font-size: 0.8rem; }
 .resize-presets { display: grid; grid-template-columns: repeat(4, 1fr); gap: 8px; }
 .resize-preset { height: 34px; border: 1px solid var(--border); border-radius: var(--radius-sm); background: var(--surface); color: var(--text-secondary); font-size: 0.78rem; font-weight: 650; cursor: pointer; }
 .resize-preset:hover,
 .resize-preset.active { border-color: var(--accent); color: var(--accent); background: var(--accent-subtle); }
 .resize-actions { display: grid; grid-template-columns: 105px 1fr; gap: 10px; align-items: center; }
 @media (max-width: 820px) {
 .resize-workspace { grid-template-columns: 1fr; }
 .resize-control-card { grid-template-columns: repeat(2, minmax(0, 1fr)); }
 .resize-actions { grid-column: 1 / -1; }
 }
 @media (max-width: 560px) {
 .tool-container { padding-left: 18px; padding-right: 18px; }
 .resize-preview-card { min-height: 0; }
 .resize-canvas-wrap { min-height: 260px; }
 .resize-control-card { grid-template-columns: 1fr; }
 .resize-actions { grid-template-columns: 1fr; }
 }
&lt;/style&gt;

&lt;div class="tool-container" id="resize-tool"&gt;
 &lt;div class="section-header"&gt;
 &lt;p class="section-label"&gt;Resize&lt;/p&gt;</description></item><item><title>Image Retouch and Mosaic Tool | Local Repair and Blur Masking - TUXIA</title><link>https://tuxiatools.com/tools/retouch.html</link><pubDate>Mon, 01 Jan 0001 00:00:00 +0000</pubDate><guid>https://tuxiatools.com/tools/retouch.html</guid><description>&lt;style&gt;
 .tool-container { max-width: 960px; margin: 0 auto; padding: 30px 24px; }
 .retouch-workspace { display: none; margin-top: 18px; border: 1px solid var(--border); border-radius: var(--radius); background: rgba(255,255,255,0.9); box-shadow: var(--shadow-sm); overflow: hidden; }
 .retouch-workspace.visible { display: block; }
 .retouch-canvas-panel { padding: 16px; }
 .retouch-canvas-wrap { min-height: 420px; border: 1px solid var(--border); border-radius: var(--radius-sm); background: linear-gradient(180deg, #fbfdff 0%, #f4f8ff 100%); display: flex; align-items: center; justify-content: center; overflow: hidden; touch-action: none; }
 #retouch-canvas { max-width: 100%; max-height: 540px; display: block; cursor: crosshair; }
 .retouch-toolbar { display: grid; gap: 14px; padding: 14px 16px 16px; border-top: 1px solid var(--border); background: #f8fbff; }
 .retouch-controls { display: grid; grid-template-columns: 150px minmax(170px, 1fr) minmax(170px, 1fr) 120px; gap: 12px; align-items: end; }
 .retouch-field { display: grid; gap: 7px; }
 .retouch-field label,
 .retouch-control-label { color: var(--text-secondary); font-size: 0.78rem; font-weight: 650; }
 .retouch-field select { height: 38px; border: 1px solid var(--border); border-radius: var(--radius-sm); background: var(--surface); color: var(--text); padding: 0 10px; }
 .retouch-field.is-hidden { display: none; }
 .retouch-range-row { display: grid; grid-template-columns: 1fr 44px; gap: 10px; align-items: center; }
 .retouch-range-row input { width: 100%; accent-color: var(--accent); }
 .retouch-range-row output { text-align: right; color: var(--text-muted); font-size: 0.76rem; }
 .retouch-actions { display: flex; flex-wrap: wrap; justify-content: center; gap: 10px; }
 .retouch-actions .btn { min-width: 96px; }
 .retouch-note { margin-top: 10px; color: var(--text-muted); font-size: 0.76rem; line-height: 1.7; }
 .retouch-status { grid-column: 1 / -1; color: var(--text-muted); font-size: 0.76rem; }
 @media (max-width: 840px) {
 .retouch-controls { grid-template-columns: 1fr 1fr; }
 .retouch-actions { justify-content: center; }
 }
 @media (max-width: 560px) {
 .tool-container { padding-left: 18px; padding-right: 18px; }
 .retouch-canvas-wrap { min-height: 300px; }
 .retouch-controls { grid-template-columns: 1fr; }
 .retouch-actions { display: grid; grid-template-columns: 1fr 1fr; }
 .retouch-actions .btn-primary { grid-column: 1 / -1; }
 }
&lt;/style&gt;

&lt;div class="tool-container" id="retouch-tool"&gt;
 &lt;div class="section-header"&gt;
 &lt;p class="section-label"&gt;Local edit&lt;/p&gt;</description></item><item><title>Image to Base64 Converter | Base64 to Image Online - TUXIA</title><link>https://tuxiatools.com/tools/base64.html</link><pubDate>Mon, 01 Jan 0001 00:00:00 +0000</pubDate><guid>https://tuxiatools.com/tools/base64.html</guid><description>&lt;style&gt;
.convert-container { max-width: 1100px; margin: 0 auto; padding: 32px 24px; }
 .split-panels { display: grid; grid-template-columns: 1fr 1fr; gap: 24px; }
 @media (max-width: 768px) { .split-panels { grid-template-columns: 1fr; } }
 .panel {
 border: 1px solid var(--border); border-radius: var(--radius);
 background: var(--surface); overflow: hidden;
 }
 .panel-header {
 padding: 12px 16px; font-weight: 600; font-size: 0.85rem;
 display: flex; align-items: center; gap: 8px;
 }
 .panel-header.left { background: #eef2ff; color: #4f46e5; }
 .panel-header.right { background: #ecfdf5; color: #059669; }
 .panel-body { padding: 16px; }

 .panel-drop-zone {
 border: 2px dashed var(--border); border-radius: var(--radius-sm);
 padding: 28px 16px; text-align: center; cursor: pointer;
 transition: all var(--transition); margin-bottom: 12px;
 }
 .panel-drop-zone:hover, .panel-drop-zone.drag-over {
 border-color: var(--accent); background: var(--accent-subtle);
 }

 .panel-preview { text-align: center; margin-bottom: 12px; }
 .panel-preview img { max-width: 100%; max-height: 200px; border-radius: var(--radius-sm); border: 1px solid var(--border); }
 .panel-meta { font-size: 0.78rem; color: var(--text-muted); margin-bottom: 12px; text-align: center; }

 .panel-textarea {
 width: 100%; min-height: 100px; max-height: 180px; padding: 10px 12px;
 border: 1px solid var(--border); border-radius: var(--radius-sm);
 font-size: 0.72rem; font-family: "SF Mono", "Fira Code", "Cascadia Code", "Consolas", "Menlo", monospace;
 resize: vertical; line-height: 1.6; word-break: break-all; outline: none; margin-bottom: 12px;
 }
 .panel-textarea:focus { border-color: var(--accent); }
 .panel-actions { display: flex; gap: 8px; flex-wrap: wrap; }
&lt;/style&gt;

&lt;!-- Tool Header --&gt;
&lt;div class="convert-container"&gt;
 &lt;div class="split-panels"&gt;
 &lt;!-- Left: Image → Base64 --&gt;
 &lt;div class="panel"&gt;
 &lt;div class="panel-header left"&gt;Image to Base64&lt;/div&gt;
 &lt;div class="panel-body"&gt;
 
 
 
 
 
 
 
 &lt;div class="panel-drop-zone" id="drop-left"&gt;
 &lt;div class="upload-icon-circle"&gt;
 
 &lt;svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="1.8"&gt;&lt;rect x="3" y="3" width="18" height="18" rx="2"/&gt;&lt;circle cx="8.5" cy="8.5" r="1.5"/&gt;&lt;polyline points="21 15 16 10 5 21"/&gt;&lt;/svg&gt;
 
 &lt;/div&gt;
 &lt;p class="upload-title-text"&gt;Drag an image here, or&lt;span&gt; click to choose&lt;/span&gt;&lt;/p&gt;</description></item><item><title>Image to URL Tool | Generate Shareable Image Links - TUXIA</title><link>https://tuxiatools.com/tools/online.html</link><pubDate>Mon, 01 Jan 0001 00:00:00 +0000</pubDate><guid>https://tuxiatools.com/tools/online.html</guid><description>&lt;style&gt;
.tool-container { max-width: 680px; margin: 0 auto; padding: 32px 24px; }
 .upload-progress { display: none; width: 100%; height: 3px; background: var(--border); border-radius: 2px; margin-top: 12px; overflow: hidden; }
 .upload-progress.active { display: block; }
 .upload-progress .bar { height: 100%; background: var(--accent); border-radius: 2px; animation: indeterminate 1.5s ease infinite; width: 28%; }
 @keyframes indeterminate { 0% { transform: translateX(-100%); } 100% { transform: translateX(420%); } }
 .url-result { display: none; align-items: center; gap: 10px; padding: 10px 13px; background: #fafaf9; border: 1px solid var(--border); border-radius: var(--radius-sm); margin-top: 12px; }
 .url-result .url-text { flex: 1; font-size: 0.78rem; font-family: "SF Mono", "Fira Code", "Cascadia Code", "Consolas", monospace; color: var(--accent); overflow: hidden; text-overflow: ellipsis; white-space: nowrap; text-decoration: none; }
&lt;/style&gt;

&lt;div class="tool-container"&gt;
 &lt;div class="section-header"&gt;
 &lt;p class="section-label"&gt;Cloud upload&lt;/p&gt;</description></item><item><title>Image Watermark Tool | Add Text or Image Watermarks Online - TUXIA</title><link>https://tuxiatools.com/tools/watermark.html</link><pubDate>Mon, 01 Jan 0001 00:00:00 +0000</pubDate><guid>https://tuxiatools.com/tools/watermark.html</guid><description>&lt;style&gt;
 .tool-container { max-width: 980px; margin: 0 auto; padding: 30px 24px; }
 .watermark-workspace { display: none; grid-template-columns: minmax(360px, 1fr) 330px; gap: 16px; align-items: stretch; margin-top: 18px; }
 .watermark-workspace.visible { display: grid; }
 .watermark-preview-card,
 .watermark-settings-card { border: 1px solid var(--border); border-radius: var(--radius); background: var(--surface); box-shadow: var(--shadow-sm); }
 .watermark-preview-card { padding: 14px; display: flex; flex-direction: column; gap: 10px; min-height: 420px; }
 .watermark-canvas-wrap { flex: 1 1 auto; min-height: 320px; border: 1px solid var(--border); border-radius: var(--radius-sm); background: linear-gradient(180deg, #fbfdff 0%, #f4f8ff 100%); display: flex; align-items: center; justify-content: center; overflow: hidden; }
 #watermark-canvas { max-width: 100%; max-height: 520px; display: block; }
 .watermark-meta { display: flex; justify-content: space-between; gap: 10px; color: var(--text-muted); font-size: 0.76rem; }
 .watermark-settings-card { padding: 16px; display: grid; gap: 13px; align-content: start; }
 .watermark-field { display: grid; gap: 7px; }
 .watermark-field label,
 .watermark-control-label { color: var(--text-secondary); font-size: 0.78rem; font-weight: 650; }
 .watermark-field input[type="text"],
 .watermark-field select { width: 100%; height: 38px; border: 1px solid var(--border); border-radius: var(--radius-sm); background: var(--surface); color: var(--text); padding: 0 11px; outline: none; }
 .watermark-field input[type="text"]:focus,
 .watermark-field select:focus { border-color: var(--accent); box-shadow: 0 0 0 3px rgba(37,99,235,0.12); }
 .watermark-inline { display: grid; grid-template-columns: 1fr 82px; gap: 10px; align-items: center; }
 .watermark-inline input[type="color"] { width: 100%; height: 38px; border: 1px solid var(--border); border-radius: var(--radius-sm); background: var(--surface); padding: 4px; }
 .watermark-range-row { display: grid; grid-template-columns: 1fr 48px; gap: 10px; align-items: center; }
 .watermark-range-row input[type="range"] { width: 100%; accent-color: var(--accent); }
 .watermark-range-row output { color: var(--text-muted); font-size: 0.76rem; text-align: right; }
 .watermark-check { display: flex; align-items: center; gap: 9px; color: var(--text-secondary); font-size: 0.8rem; }
 .watermark-logo-row { display: grid; grid-template-columns: 1fr auto; gap: 10px; align-items: center; }
 .watermark-logo-name { min-width: 0; color: var(--text-muted); font-size: 0.76rem; white-space: nowrap; overflow: hidden; text-overflow: ellipsis; }
 .watermark-actions { display: grid; grid-template-columns: 110px 1fr; gap: 10px; align-items: center; margin-top: 4px; }
 @media (max-width: 860px) {
 .watermark-workspace { grid-template-columns: 1fr; }
 .watermark-settings-card { grid-template-columns: repeat(2, minmax(0, 1fr)); }
 .watermark-actions { grid-column: 1 / -1; }
 }
 @media (max-width: 560px) {
 .tool-container { padding-left: 18px; padding-right: 18px; }
 .watermark-preview-card { min-height: 0; }
 .watermark-canvas-wrap { min-height: 260px; }
 .watermark-settings-card { grid-template-columns: 1fr; }
 .watermark-actions { grid-template-columns: 1fr; }
 }
&lt;/style&gt;

&lt;div class="tool-container" id="watermark-tool"&gt;
 &lt;div class="section-header"&gt;
 &lt;p class="section-label"&gt;Watermark editor&lt;/p&gt;</description></item><item><title>Online Image Cropper | Crop Photos, Ratios and Rotation - TUXIA</title><link>https://tuxiatools.com/tools/crop.html</link><pubDate>Mon, 01 Jan 0001 00:00:00 +0000</pubDate><guid>https://tuxiatools.com/tools/crop.html</guid><description>&lt;style&gt;
.tool-container { max-width: 900px; margin: 0 auto; padding: 32px 24px; }
 .crop-section { display: none; margin-top: 22px; }
 .crop-panel { border: 1px solid var(--border); border-radius: var(--radius); background: var(--surface); padding: 18px; box-shadow: var(--shadow-sm); }
 .crop-toolbar { display: flex; align-items: center; justify-content: space-between; gap: 14px; flex-wrap: wrap; margin-bottom: 16px; }
 .ratio-control { display: flex; align-items: center; gap: 10px; flex-wrap: wrap; }
 .ratio-label { font-size: 0.8rem; font-weight: 600; color: var(--text-secondary); }
 .ratio-btns { display: flex; gap: 6px; flex-wrap: wrap; }
 .ratio-btn { padding: 6px 11px; border: 1px solid var(--border); border-radius: var(--radius-sm); cursor: pointer; font-size: 0.76rem; font-weight: 600; background: var(--surface); transition: all var(--transition); }
 .ratio-btn:hover, .ratio-btn.active { border-color: var(--accent); background: var(--accent-subtle); color: var(--accent); }
 .crop-actions { display: flex; gap: 8px; flex-wrap: wrap; justify-content: flex-end; }
 .crop-canvas-wrap { position: relative; display: flex; justify-content: center; align-items: center; min-height: 260px; padding: 14px; border: 1px solid #dbeafe; border-radius: var(--radius); background: linear-gradient(45deg, #f8fafc 25%, transparent 25%), linear-gradient(-45deg, #f8fafc 25%, transparent 25%), linear-gradient(45deg, transparent 75%, #f8fafc 75%), linear-gradient(-45deg, transparent 75%, #f8fafc 75%); background-size: 18px 18px; background-position: 0 0, 0 9px, 9px -9px, -9px 0; overflow: auto; }
 #crop-canvas { max-width: 100%; display: block; border-radius: var(--radius-sm); box-shadow: 0 12px 28px rgba(15,23,42,0.12); touch-action: none; user-select: none; }
 .crop-tip { margin-top: 10px; font-size: 0.76rem; color: var(--text-muted); text-align: center; }
 .preview-result { text-align: center; margin-top: 20px; display: none; }
 .preview-result img { max-width: 100%; max-height: 300px; border-radius: var(--radius-sm); border: 1px solid var(--border); box-shadow: var(--shadow-sm); }
 @media (max-width: 680px) {
 .crop-toolbar { align-items: stretch; }
 .crop-actions { justify-content: flex-start; }
 }
&lt;/style&gt;

&lt;div class="tool-container"&gt;
 &lt;div class="section-header"&gt;
 &lt;p class="section-label"&gt;Crop editor&lt;/p&gt;</description></item><item><title>QR Code Decoder | Read QR Code Images Online - TUXIA</title><link>https://tuxiatools.com/tools/qrcode-dec.html</link><pubDate>Mon, 01 Jan 0001 00:00:00 +0000</pubDate><guid>https://tuxiatools.com/tools/qrcode-dec.html</guid><description>&lt;style&gt;
.tool-container { max-width: 680px; margin: 0 auto; padding: 32px 24px; }
 .result-card { display: none; padding: 20px; margin-top: 20px; background: var(--surface); border: 1px solid var(--border); border-radius: var(--radius); box-shadow: var(--shadow-sm); }
 .result-card.visible { display: block; }
 .result-content { font-size: 1rem; font-weight: 600; word-break: break-all; padding: 10px 14px; background: #fafaf9; border: 1px solid var(--border); border-radius: var(--radius-sm); margin-bottom: 14px; }
 .result-actions { display: flex; gap: 8px; flex-wrap: wrap; }
 .result-type { display: inline-block; font-size: 0.7rem; padding: 2px 8px; border-radius: var(--radius-full); font-weight: 500; margin-bottom: 8px; }
 .type-url { background: #eef2ff; color: var(--accent); }
 .type-text { background: #ecfdf5; color: var(--success); }
 .preview-img-wrap { text-align: center; margin: 16px 0; }
 .preview-img-wrap img { max-width: 260px; max-height: 260px; border-radius: var(--radius-sm); border: 1px solid var(--border); }
&lt;/style&gt;

&lt;div class="tool-container"&gt;
 &lt;div class="section-header"&gt;
 &lt;p class="section-label"&gt;Decode&lt;/p&gt;</description></item><item><title>QR Code Generator | Create QR Codes with Logo and Styles - TUXIA</title><link>https://tuxiatools.com/tools/qrcode-gen.html</link><pubDate>Mon, 01 Jan 0001 00:00:00 +0000</pubDate><guid>https://tuxiatools.com/tools/qrcode-gen.html</guid><description>&lt;style&gt;
.tool-container { max-width: 1000px; margin: 0 auto; padding: 32px 24px; }
 .qr-layout { display: grid; grid-template-columns: 300px 1fr; gap: 28px; align-items: start; }
 @media (max-width: 700px) { .qr-layout { grid-template-columns: 1fr; } }
 .settings-card { background: var(--surface); border: 1px solid var(--border); border-radius: var(--radius); padding: 18px; }
 .setting-group { margin-bottom: 14px; }
 .setting-group label { display: block; font-size: 0.8rem; font-weight: 500; color: var(--text-secondary); margin-bottom: 4px; }
 .setting-group input[type="text"], .setting-group textarea, .setting-group select {
 width: 100%; padding: 7px 11px; border: 1px solid var(--border); border-radius: var(--radius-sm);
 font-size: 0.82rem; font-family: inherit; background: var(--surface-soft); color: var(--text); outline: none;
 }
 .setting-group input:focus, .setting-group textarea:focus, .setting-group select:focus { border-color: var(--accent); }
 .setting-group input[type="color"] { width: 36px; height: 32px; border: 1px solid var(--border); border-radius: var(--radius-sm); cursor: pointer; padding: 2px; }
 .setting-group input[type="range"] { width: 100%; }
 .color-row { display: grid; grid-template-columns: 42px minmax(0, 1fr); gap: 8px; align-items: center; }
 .color-row input[type="color"] { width: 42px; height: 34px; background: var(--surface); }
 .color-row input[type="text"] {
 height: 34px;
 min-width: 0;
 font-family: "SF Mono", "Cascadia Code", Consolas, monospace;
 font-weight: 700;
 letter-spacing: 0;
 text-transform: uppercase;
 }
 :root[data-theme="dark"] .color-row input[type="text"] {
 background: #f8fbff;
 color: #0f172a;
 border-color: #9db8d8;
 }
 :root[data-theme="dark"] .color-row input[type="text"]::placeholder { color: #64748b; }
 .preview-area { text-align: center; }
 #qr-container { display: inline-block; padding: 20px; background: #fff; border-radius: var(--radius); border: 1px solid var(--border); box-shadow: var(--shadow); }
 .download-actions { margin-top: 16px; display: flex; gap: 10px; justify-content: center; flex-wrap: wrap; }

 /* Dot style buttons */
 .dot-style-opts { display: flex; gap: 6px; flex-wrap: wrap; }
 .dot-style-btn { flex: 0 0 auto; padding: 5px 10px; border: 1px solid var(--border); border-radius: var(--radius-sm); cursor: pointer; font-size: 0.7rem; font-weight: 500; background: var(--surface); transition: all var(--transition); }
 .dot-style-btn:hover { border-color: var(--accent); }
 .dot-style-btn.active { border-color: var(--accent); background: var(--accent-subtle); color: var(--accent); }

 /* Logo upload */
 .logo-upload-row { display: flex; gap: 8px; align-items: center; }
 .logo-preview-mini { width: 28px; height: 28px; border-radius: 4px; border: 1px solid var(--border); object-fit: contain; display: none; }
 #logo-input { display: none; }

 /* Preset templates */
 .preset-grid { display: flex; gap: 6px; flex-wrap: wrap; }
 .preset-dot { width: 26px; height: 26px; border-radius: 4px; cursor: pointer; border: 2px solid transparent; background: var(--surface); display: flex; align-items: center; justify-content: center; font-size: 0.7rem; transition: all var(--transition); }
 .preset-dot:hover { border-color: var(--accent); }
 .preset-dot.active { border-color: var(--accent); background: var(--accent-subtle); }
 .preset-dot .dot { width: 8px; height: 8px; border-radius: 50%; background: #000; }
 .preset-dot .dot.sq { border-radius: 1px; }
 .preset-dot .dot.ro { border-radius: 3px; }
&lt;/style&gt;

&lt;div class="tool-container"&gt;
 &lt;div class="section-header"&gt;
 &lt;p class="section-label"&gt;QR code tool&lt;/p&gt;</description></item></channel></rss>